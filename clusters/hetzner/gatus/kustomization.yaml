apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/gatus
  - namespace.yaml
  - configmap.yaml
  - ingress.yaml
  - sealed-gatus-security.yaml

patches:
  # k3s lacks network storage; use node-local provisioner
  - target:
      kind: PersistentVolumeClaim
      name: gatus-data
    patch: |
      - op: add
        path: /spec/storageClassName
        value: local-path
