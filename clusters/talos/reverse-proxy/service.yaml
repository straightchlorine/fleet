apiVersion: v1
kind: Service
metadata:
  name: nginx-reverse-proxy
  namespace: reverse-proxy
  labels:
    app: nginx-reverse-proxy
spec:
  type: NodePort
  ports:
    - name: https
      port: 443
      targetPort: 8443
      nodePort: 443
      protocol: TCP
  selector:
    app: nginx-reverse-proxy
