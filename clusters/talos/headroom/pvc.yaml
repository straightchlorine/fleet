apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: headroom-state
  namespace: headroom
  labels:
    app: headroom
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
