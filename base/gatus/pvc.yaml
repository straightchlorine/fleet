apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gatus-data
  namespace: gatus
  labels:
    app: gatus
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi
